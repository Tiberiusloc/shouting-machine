window.onload = function() {

  let form = document.querySelectory('form');
  form.onsubmit = fucntion(event) {

  event.preventDefault();
  const personInput = document.getElementById("personInput").value;
  
  }
}